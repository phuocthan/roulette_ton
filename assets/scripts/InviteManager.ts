import { _decorator, Component, Node } from 'cc';
import BaseScreen from './BaseScreen';
const { ccclass, property } = _decorator;

@ccclass('InviteManager')
export class InviteManager extends BaseScreen {
    start() {

    }

    update(deltaTime: number) {
        
    }
}


