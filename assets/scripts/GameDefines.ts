export enum GameConst {
    // reward time countdown in minutes
    // default 180 minutes = 3h
    FREE_REWARD_COUNTDOWN_TIME = 180,
    // init coins
    INIT_GAME_COINS = 5000,
    // reward coins num after FREE_REWARD_COUNTDOWN_TIME
    FREE_REWARD_COINS = 2500
}